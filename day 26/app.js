let word=`{"fact":"A cat has more bones than a human being; humans have 206 and the cat has 230 bones.","length":83}`
let validResp=JSON.parse(word);
console.log(validResp);

let student = {
    rollno: 13234727,
};